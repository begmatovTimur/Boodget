/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Optional, uncomment if you want clean `/page/` URLs instead of `/page.html`
    // trailingSlash: true,
}

module.exports = nextConfig
